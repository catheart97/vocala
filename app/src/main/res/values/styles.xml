<resources>

    <!-- Base application theme. -->
    <style name="Vocala"
           parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="colorAccent">@android:color/darker_gray</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="android:colorBackground">@color/material_dark</item>
    </style>

    <!-- Storage Dialog Anim -->
    <style name="VocalaDialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- Splash Theme -->
    <style name="Vocala.Launcher"
           parent="Vocala">
        <item name="android:windowBackground">?attr/colorAccent</item>
        <item name="android:colorBackground">?attr/colorAccent</item>
        <item name="android:navigationBarColor">?attr/colorAccent</item>
        <item name="colorAccent">@color/ic_launcher_background</item>
    </style>

</resources>
